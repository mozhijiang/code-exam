import { View } from "@tarojs/components";
import { FC } from "react";
import { useTheme } from "../../utils/useHooks";

const CatelogList: FC = () => {
    const theme = useTheme();
    return (
        <View>
            this is catelogs pages...
        </View>
    );
}
export default CatelogList;